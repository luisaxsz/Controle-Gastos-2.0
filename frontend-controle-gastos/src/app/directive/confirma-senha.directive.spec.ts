import { ConfirmaSenhaDirective } from './confirma-senha.directive';

describe('ConfirmaSenhaDirective', () => {
  it('should create an instance', () => {
    const directive = new ConfirmaSenhaDirective();
    expect(directive).toBeTruthy();
  });
});
